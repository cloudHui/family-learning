---
description: 本地测试与临时文件必须落在项目 temp/ 目录
alwaysApply: true
---

# 本地测试约定

凡启动服务、跑回归、造测试数据、写日志，一律在本仓库内完成，禁止使用 `/tmp` 或其他仓库外路径。

## 目录

- 根目录：`temp/`（可整目录删除）
- 数据：`temp/data/`
- 资源：`temp/resources/`
- 数据集：`temp/datasets/`（或 `temp/data/datasets/`）
- 日志：`temp/logs/`

## 要求

1. 集成测试、脚本、手工验证都指向上述路径。
2. 测完可清理 `temp/`（保留 `.gitkeep` 即可）；不要留下仓库外残留。
3. 每次排查/修功能问题都要跑相关回归，并确认能在项目目录内启动通过。
4. `.gitignore` 已忽略 `temp/` 内容；勿把测试产物提交进库。
